Ny bestilling for:

Hytte    : ${booking.hut.name}
Startdato: ${booking.startDate}
Sluttdato: ${booking.endDate}

av

Navn  : ${booking.contact.name}
E-post: ${booking.contact.email}
Tlf   : ${booking.contact.phone}
